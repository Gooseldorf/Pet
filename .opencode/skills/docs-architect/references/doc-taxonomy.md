# Doc Taxonomy

Use this taxonomy when deciding where information belongs.

## Project Map

Use `docs/project-map.md` for:

- repository structure
- top-level path ownership
- source-of-truth index

## AI Navigation

Use `docs/ai/` for:

- retrieval entrypoints
- task-to-document routing
- agent navigation aids

## Systems

Use `docs/systems/` for:

- CI/CD
- deployment
- infrastructure
- concrete project systems

## Unity

Use `docs/unity/` for:

- scenes
- prefabs
- authored asset layout
- Unity-specific structure

## Workflows

Use `docs/workflows/` for:

- step-by-step procedures
- maintenance routines
- repeatable contributor actions

## History

Use `docs/history/` for:

- milestones
- durable historical facts
- notable completed technical steps
