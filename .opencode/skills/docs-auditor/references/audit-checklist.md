# Audit Checklist

Check for the following:

- every major topic has a clear source-of-truth page
- navigation docs point to those pages
- repository paths are concrete and real
- milestone history captures major completed work
- no two pages appear to own the same topic
- `AGENTS.md` stays short and points to `docs/` rather than duplicating it
